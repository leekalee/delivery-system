spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/delivery
    username: user
    password: password
    profiles:
      active: dev
    security:
        user:
          name: admin
          password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true